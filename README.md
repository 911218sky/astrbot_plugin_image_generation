# 通用生圖插件

![:name](https://count.getloli.com/@astrbot_plugin_image_generation?name=astrbot_plugin_image_generation&theme=miku&padding=7&offset=0&align=top&scale=1&pixelated=1&darkmode=auto)

### 支援多種主流生圖接口的通用圖像生成插件，支援文生圖、圖生圖、LLM 自動呼叫，以及可自訂的預設系統。

> 外掛識別名稱：`astrbot_plugin_image_generation_911218sky`

## 致謝

本插件基於原作者 [Railgun19457](https://github.com/railgun19457) 的
[`astrbot_plugin_image_generation`](https://github.com/railgun19457/astrbot_plugin_image_generation)
進行修改與延伸，原始設計、整體架構與多適配器生圖能力都來自原作者的工作。

## 這個版本的調整

相較於原版，這個版本主要做了以下修改：

- 將插件內大部分對外文字、提示訊息與設定說明調整為繁體中文
- OpenAI 生圖流程改為只保留 GPT Image 系列，不再保留 DALL-E 分支
- 強化 LLM Tool 的說明與系統提示，讓模型更容易在有生圖意圖時自動呼叫工具
- 調整 Telegram 指令描述與指令刷新流程，讓 `/` 指令提示更容易正確出現
- 加入可由使用者設定、且上限為 5 次的重試機制與終端日誌提示，提升失敗場景的可觀察性
- 提高供應商 Base64 JSON 回應上限，避免 2K/4K 圖片因回應較大被誤判為格式錯誤；固定格式錯誤不再重試
- 移除內建預設提示詞，改為由使用者自行維護預設內容
- 參考 LivingMemory 的指令組寫法，將生圖、任務、模型與預設管理統一整理到 `/img` 指令組
- 新增 AstrBot 官方插件 Pages 看板，可查看排隊中、生成中、最近紀錄、模型狀態與照片快取，並支援快取圖片預覽與下載

***

### 命令
- `/img gen <提示詞或預設名稱> [額外提示詞]`
  - 生成圖片。示例: `/img gen 一隻可愛的小貓`
  - 使用預設。示例：`/img gen 動漫風`
  - 使用預設並附加額外提示詞。示例: `/img gen 動漫風 藍色髮`
  - **圖生圖模式**：如果訊息中包含圖片、引用包含圖片的訊息，或 @ 使用者，將自動作為參考圖進入圖生圖模式（需適配器支援）。
  - @ 使用者時會取得其頭像作為參考圖。示例: `/img gen 像素風 @使用者A`
  - 也可使用 `/img generate`、`/img create`、`/img draw` 作為生圖別名。
  - 批量變體：`/img gen --count 3 <提示詞>` 或 `/img gen --count=3 <提示詞>`；上限由 `generation.max_batch_count` 控制。

- 可在提示詞中直接加入 `@self`，將當前 Bot 的頭像作為參考圖。示例：`/img gen 像素風 @self`

- `/img model`
  - 顯示可用模型列表和當前使用的模型。模型格式為 `供應商名稱/模型名稱`。
  - `/img model <序號>` - 切換到指定序號的模型。

- `/img tasks`
  - 查看目前正在進行中的生圖任務。
  - 會顯示任務 ID、任務類型、執行秒數、比例、解析度與提示詞摘要。

- `/img preset`
  - 顯示目前所有預設。
  - `/img preset add <預設名:預設內容>` - 新增預設。
  - `/img preset del <預設名>` - 刪除指定預設。

- `/img help`
  - 顯示 `/img` 指令組說明。

### 功能特性

- ✅ **多供應商配置**：支援配置多個生圖服務供應商，每個供應商可擁有獨立的 API Key、Base URL 及模型列表。
- ✅ **模型動態切換**：透過指令即時檢視和在不同供應商的模型間切換。
- ✅ **供應商/模型體系**：採用 `供應商名稱/模型名稱` 的統一標識體系。
- ✅ **文生圖**：根據文字描述生成圖片。
- ✅ **圖生圖**：基於參考圖片（支援多張）生成新圖片，支援自動識別訊息圖片、引用圖片及 @ 使用者頭像。
- ✅ **LLM 整合**：支援作為工具被 LLM 自動呼叫，並可依自然語言需求主動判斷較合適的寬高比與解析度。
- ✅ **Web 任務看板**：在 AstrBot 插件頁查看排隊中/生成中任務、最近結果、照片快取、模型設定摘要；照片快取支援分頁、預覽與下載。
- ✅ **進階預設系統**：支援自訂提示詞模板，預設內容支援 JSON 格式以配置特定比例和解析度。
- ✅ **多 API Key 輪詢**：支援配置多個 API Key，自動輪詢與錯誤重試（次數可設定，最高 5 次）；明確的 4xx 參數或認證錯誤會立即停止，避免無效等待。
- ✅ **完善的控制**：支援併發任務限制、使用者頻率限制（Rate Limit）及每日生圖數量限制。
- ✅ **低壓力處理**：大型回應採單路解析，且會在 Base64 解碼前套用圖片大小限制，降低高解析度批量生圖的記憶體峰值。
- ✅ **批量生圖**：AI 工具可用 `count` 請求多張獨立變體；插件會並行執行、保留輸出順序，部分失敗會明確回報。

### LLM 工具調用與效能

LLM 工具名稱是 `generate_image`。主模型需要支援工具呼叫，插件會在每次 LLM 請求加入生圖意圖提示；當模型決定呼叫工具後，工具先完成黑名單、額度、併發與參考圖檢查，再建立背景任務並立即返回，不會讓主對話等待圖片下載完成。

單張請求以一個生成工作為單位；若供應商回傳可重試的傳輸或伺服器錯誤，會依 `max_retry_attempts` 與退避設定重試。批量請求會將 `count` 拆成獨立變體並行執行，並受 `generation.max_concurrent_tasks` 限制；輸出按照請求順序發布，部分失敗會保留成功圖片並回報失敗數量。實際首圖時間仍取決於供應商 API、模型解析度、網路與重試設定。

若 LLM 沒有呼叫工具，先確認目前對話模型支援 tools、`enable_llm_tool` 已開啟，並檢查系統提示沒有要求模型只回覆文字。若工具回傳「已建立文生圖任務」，代表任務已排入背景流程；完成後圖片會另行發送。

### 適配器支援狀態

| 適配器型別      | 文生圖 | 圖生圖 | 解析度 | 寬高比 | 說明                                                                  |
| :-------------- | :----: | :----: | :----: | :----: | :-------------------------------------------------------------------- |
| `gemini`        |   ✅    |   ✅    |   ✅    |   ✅    | Gemini 原生 API                                                       |
| `gemini_openai` |   ✅    |   ✅    |   ❌    |   ❌    | OpenAI 相容格式的 Gemini 接口                                         |
| `openai`        |   ✅    |   ✅    |   ❌    |   ✅    | OpenAI GPT Image 系列（gpt-image-1、gpt-image-1-mini、gpt-image-1.5 等） |
| `z_image_gitee` |   ✅    |   ❌    |   ✅    |   ✅    | Gitee AI (z-image-turbo)                                              |
| `jimeng2api`    |   ✅    |   ✅    |   ✅    |   ✅    | 適用於[iptag/jimeng-api](https://github.com/iptag/jimeng-api)的適配器 |
持續更新中······
#### 特殊說明
  - 配置了jimeng2api的話，會在每次啟動時和每天凌晨自動領取積分(僅限直接連線即夢逆向,中轉途徑沒有對應接口)
  - jimeng逆向使用了/v1/images/compositions接口,使用中轉會導致圖生圖失敗
#### 歡迎提交 Issue/PR 新增新的適配器型別

### 配置項

#### 基礎配置
| 配置項            | 型別    | 預設值 | 說明                                  |
| :---------------- | :------ | :----- | :------------------------------------ |
| `enable_llm_tool` | boolean | `true` | 是否允許 LLM 在對話中自動呼叫生圖功能 |

#### API 供應商配置 (`api_providers`)
支援配置多個供應商，每個供應商項包含：
| 配置項             | 型別   | 說明                                                                 |
| :----------------- | :----- | :------------------------------------------------------------------- |
| `name`             | string | 適配器名稱，用於區分不同供應商。生圖模型格式為 `適配器名稱/模型名稱` |
| `base_url`         | string | API Base URL                                                         |
| `proxy`            | string | 代理地址，例如 `http://127.0.0.1:7890`                               |
| `api_keys`         | list   | API Keys 列表                                                        |
| `available_models` | list   | 可用模型列表，用於 `/img model` 命令切換                             |

#### 生成行為配置 (`generation`)
`generation.max_queued_tasks` 對應執行期的 `generation_settings.max_queued_tasks`，只計算等待執行的任務，不包含已取得併發名額的任務。

| 配置項                 | 型別    | 預設值   | 說明                                                                                                       |
| :--------------------- | :------ | :------- | :--------------------------------------------------------------------------------------------------------- |
| `model`                | string  | `""`     | 當前使用的模型，格式為 `供應商名稱/模型名稱`，不填寫預設使用第一個可用模型，可以透過 `/img model` 指令更新 |
| `timeout`              | number  | `180`    | 請求超時時間（秒）。                                                                                       |
| `max_retry_attempts`   | number  | `5`      | 最大重試次數，可自行設定，最高為 5。                                                                       |
| `max_concurrent_tasks` | number  | `3`      | 最大併發生圖任務數，防止資源耗盡；高解析度圖片建議設定為 1 到 3。                                           |
| `max_queued_tasks`     | number  | `6`      | 最大排隊任務數；併發與佇列都滿時會立即拒絕新任務。                                                         |
| `max_batch_count`      | number  | `4`      | 單次 AI 或命令最多請求的圖片數量，限制為 1 到 10；每日額度按成功圖片數扣除。                              |
| `default_aspect_ratio` | string  | `"自動"` | 預設圖片寬高比。可選值：`自動`、`1:1`、`2:3`、`3:2`、`3:4`、`4:3`、`4:5`、`5:4`、`9:16`、`16:9`、`21:9`。  |
| `default_resolution`   | string  | `"1K"`   | 預設解析度（僅部分模型支援）。可選值：`1K`、`2K`、`4K`。                                                   |
| `show_task_started`     | boolean | `false`  | 是否在 `/img gen` 命令與 AI 呼叫 `generate_image` 時傳送任務啟動提示；關閉後仍會正常執行背景生圖任務。       |
| `show_generation_info` | boolean | `false`  | 生圖成功後展示耗時和生成數量，可於 Web 設定中開啟或關閉。                                                 |
| `show_model_info`      | boolean | `false`  | 生圖成功後展示所使用的模型名稱。                                                                           |

#### 使用者限制配置 (`user_limits`)
| 配置項                    | 型別    | 預設值                       | 說明                                                             |
| :------------------------ | :------ | :--------------------------- | :--------------------------------------------------------------- |
| `umo_blacklist`           | list    | `[]`                         | 會話黑名單，黑名單中的會話將無法使用生圖功能(包含指令和LLM Tool) |
| `blacklist_block_message` | string  | `"❌ 當前會話未啟用生圖功能"` | 當黑名單會話使用生圖功能時返回的文字，留空則不返回任何內容       |
| `rate_limit_seconds`      | number  | `0`                          | 單個使用者兩次請求之間的最小間隔時間（秒），0 表示不限制。         |
| `max_image_size_mb`       | number  | `10`                         | 參考圖與生成圖片的最大檔案大小（MB）；超過此大小的圖片不會儲存或傳送。 |
| `enable_daily_limit`      | boolean | `false`                      | 是否啟用每日生圖數量限制。                                       |
| `daily_limit_count`       | number  | `10`                         | 每個使用者每日允許生成的最大圖片數量。                             |

#### 參考圖安全邊界

- 本地參考圖只允許位於插件資料目錄、AstrBot `data/temp` 或 `data/attachments`。讀取會沿已核准根目錄使用檔案描述符逐層開啟；路徑越界、符號連結、硬連結、讀取期間檔案身分改變，或平台缺少必要的描述符防護時，都會拒絕整批參考圖，不會退回遠端下載。
- 遠端參考圖只接受 `http`/`https`，且每次連線與每次重新導向都必須重新解析為全公開 IP。DNS 答案混入私有或非公開位址、DNS rebinding、實際連線端點與解析結果不符，或無法證明端點安全時，都會拒絕請求。遠端讀取最多跟隨 4 次重新導向，並限制連線數、逾時與下載大小；不採用環境代理設定。

#### 安全稽核配置 (`safety_audit`)
支援生圖前提示詞稽核和生圖後圖片稽核：
**白名單會話**
| 配置項          | 型別 | 預設值 | 說明                                                                |
| :-------------- | :--- | :----- | :------------------------------------------------------------------ |
| `umo_whitelist` | list | `[]`   | 稽核白名單，白名單中的會話可以跳過稽核流程,使用"sid"指令取得會話umo |
**提示詞稽核 (`prompt_audit`)**
| 配置項            | 型別    | 預設值     | 說明                                                                  |
| :---------------- | :------ | :--------- | :-------------------------------------------------------------------- |
| `blocked_words`   | list    | `[]`       | 自定義遮蔽詞列表，命中任意詞將直接拒絕。                              |
| `enable_ai_audit` | boolean | `false`    | 是否啟用 AI 提示詞稽核。                                              |
| `ai_provider_id`  | string  | `""`       | 稽核使用的 Provider ID。留空時使用當前會話的對話模型 Provider。       |
| `ai_prompt`       | string  | 見預設配置 | AI 稽核提示詞，支援 `{prompt}` 佔位符；未包含時會自動附加當前提示詞。 |

**圖片稽核 (`image_audit`)**
| 配置項            | 型別    | 預設值     | 說明                                                                |
| :---------------- | :------ | :--------- | :------------------------------------------------------------------ |
| `enable_ai_audit` | boolean | `false`    | 是否啟用 AI 圖片稽核。                                              |
| `ai_provider_id`  | string  | `""`       | 稽核使用的 Provider ID。留空時使用當前會話的對話模型 Provider。     |
| `ai_prompt`       | string  | 見預設配置 | AI 稽核提示詞；僅在顯式寫入 `{prompt}` 佔位符時才會附帶當前提示詞。 |

預設情況下，`prompt_audit.ai_prompt` 會把當前生圖提示詞注入到 `{prompt}` 中。
`image_audit.ai_prompt` 預設只稽核圖片本身；只有顯式寫入 `{prompt}` 時才會把當前提示詞一併傳給稽核模型。

#### 快取配置 (`cache`)
| 配置項                   | 型別   | 預設值 | 說明                                       |
| :----------------------- | :----- | :----- | :----------------------------------------- |
| `max_cache_count`        | number | `100`  | 最大快取圖片數量，超過此數量將清理舊圖片。 |
| `cleanup_interval_hours` | number | `24`   | 定時清理間隔（小時）。                     |

### AstrBot 插件頁看板

本插件提供 `pages/dashboard/` 作為 AstrBot 官方插件 Pages 介面。進入 AstrBot WebUI 的插件頁後，可以查看：

- 目前正在排隊或生成中的任務、任務 ID、會話、提示詞摘要、比例、解析度與參考圖數量。
- 進行中批量任務會即時顯示完成數、成功/失敗數、目前階段、供應商耗時與總耗時，頁面約每 2 秒更新一次。
- 最近 50 筆完成、失敗或審核阻擋紀錄。
- 分頁查看快取目錄中的圖片檔案、模型/限制設定摘要，並可放大預覽與下載原圖。
- 新生成圖片的模型、提示詞、任務 ID、會話與狀態。

看板不修改 AstrBot 核心，也不寫入 AstrBot 核心資料庫。進行中任務與最近紀錄來自插件執行期快照，服務重啟後會清空；已儲存在快取目錄的圖片檔案會依快取設定保留。

新生成圖片的模型與提示詞會記錄在插件資料目錄的 `image_metadata.json`，用來讓照片快取頁在重啟後仍能顯示圖片來源。舊圖片或手動放入的圖片如果沒有 metadata，頁面會標示為未記錄。

照片快取頁採用分頁載入，預設每頁 12 張，只有目前頁面的圖片會產生縮圖；任務總覽自動刷新時不會重複載入大量圖片，避免快取很多時拖慢 AstrBot WebUI。

#### 預設配置 (`presets`)
| 配置項    | 型別 | 預設值           | 說明                                       |
| :-------- | :--- | :--------------- | :----------------------------------------- |
| `presets` | list | 空列表 | 可自訂的圖片生成預設，詳見下方說明。 |

**預設格式說明：**
- **簡單格式**：`名稱:提示詞`，例如 `動漫風:anime style`
- **高階格式（JSON）**：`名稱:{"prompt":"提示詞","aspect_ratio":"16:9","resolution":"2K","description":"描述"}`

> 本版本不提供任何內建預設，`presets` 預設為空，請依需求自行新增。


### 使用示例

#### 基礎生圖
```
/img gen 一隻在森林裡野餐的兔子
```

#### 使用預設
```
/img preset add 賽博朋克:cyberpunk style, neon lights, futuristic city
/img gen 賽博朋克 貓
```

#### 圖生圖（引用或 @）
1. 傳送或引用一張圖片。
2. 輸入 `/img gen 變成動漫風格[圖片][引用訊息]`。
或者：
`/img gen 變成像素風 @使用者A` (將使用使用者A的頭像作為參考圖)

#### 切換模型
```
/img model        # 檢視列表
/img model 2      # 切換到第二個模型
```

### 更新日誌
- **v1.0.11-2026-07-17**
  - 新增獨立的最大排隊任務數，併發與佇列都滿時會在下載參考圖或呼叫供應商前拒絕。
  - 本地參考圖限於核准資料目錄並採用描述符安全讀取；遠端參考圖加入公開 IP、DNS、連線端點、重新導向、大小與逾時限制。
  - 強化安全稽核檔案清理，照片快取頁只顯示已成功生成的圖片。

- **v1.0.10-2026-06-07**
  - Dashboard 曾將自動刷新間隔由 5 秒調整為 30 秒；目前進行中任務會以 2 秒間隔更新進度。
  - 手動重新整理仍會立即更新總覽與目前照片快取頁。

- **v1.0.9-2026-06-07**
  - 照片快取頁改為分頁載入，可選每頁 12/24/36 張。
  - 總覽自動刷新不再附帶快取圖片縮圖，降低圖片很多時的 WebUI 壓力。

- **v1.0.8-2026-06-07**
  - 照片快取頁新增原圖放大預覽與下載。
  - 新生成圖片會在插件自己的 `image_metadata.json` 記錄模型、提示詞、任務 ID、會話與狀態。
  - 快取圖卡與預覽視窗會顯示模型、提示詞摘要與任務資訊。

- **v1.0.7-2026-06-07**
  - 新增 AstrBot 官方插件 Pages 看板，可查看排隊中、生成中、最近紀錄與照片快取。
  - 生圖任務狀態新增 queued/running/success/failed/blocked 快照。

- **v1.0.6-2026-06-07**
  - 將生圖、任務、模型與預設管理統一整理為 `/img` 指令組：`/img gen`、`/img tasks`、`/img model`、`/img preset`、`/img help`。

- **v1.0.5-2026-05-08**
  - LLM Tool 啟動圖生圖任務時，通知訊息現在會顯示參考圖數量。

- **v1.0.4-2026-05-07**
  - 新增會話黑名單
  - 新增稽核白名單會話
  - 新增安全稽核功能
  - 新增 Grok AI 適配器
  - OpenAI 生圖流程改為僅保留 GPT Image 系列
  - 移除內建預設提示詞
  - 補齊繁體中文介面與提示訊息
  - 強化 LLM Tool 說明與系統提示，讓模型更容易自動呼叫生圖工具
  - LLM Tool 現在會依自然語言內容主動判斷較合適的寬高比與解析度
  - 新增生成開始通知與失敗通知
  - 新增 `/img_tasks` 指令，可查看目前正在進行中的生圖任務
  - 新增成功耗時提示開關，可在 Web 設定中控制
  - 重試次數改為可設定，最高 5 次，並補充終端日誌提示
  - 外掛識別名稱改為 `astrbot_plugin_image_generation_911218sky`，避免與原作者同名外掛發生更新比對衝突

## 原作者與來源

- 原作者：Railgun19457
- 原始專案：<https://github.com/railgun19457/astrbot_plugin_image_generation>
